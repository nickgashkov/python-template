Python backbone for various projects.

# How to use

1. Clone this repo via `git clone`
2. Execute `make nameit NAME=yourprojectname`
3. Remove this repo git data `rm -rf .git`...
4. ...and initialize git `git init`
